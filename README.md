# chip8 emulator

## How to build
```
cmake -B build
cmake --build build
```
It installs SDL3, so it may take some time to build

## How to run
```
./build/chip8-emulator random_rom_downloaded_from_trustworthy_internet.ch8
```
